# docker-compose.prod.yml için proje kökünde kullanılacak .env örneği
# Kopyalayın: cp deploy/.env.prod.example .env
# Sonra değerleri düzenleyin.

# Frontend'in API adresi (tarayıcıdan erişilen URL)
NEXT_PUBLIC_API_URL=https://yourdomain.com

# İsteğe bağlı: farklı Postgres şifre/kullanıcı (varsayılan: metaads)
# POSTGRES_USER=metaads
# POSTGRES_PASSWORD=güçlü_şifre
# POSTGRES_DB=metaads
